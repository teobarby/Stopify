# Metti qui le immagini referenziate dai file .tex
#
# File attesi (vedi README.md per dettagli):
#
#   architettura-deployment.png    — schema di deployment in produzione
#   use-case-diagram.png           — Use Case Diagram UML
#   class-diagram.png              — Class Diagram UML
#   architettura-blocchi.png       — diagramma a blocchi del sistema
#   schema-db.png                  — schema ER del database
#
# Tool consigliato: draw.io (https://app.diagrams.net)
# Formato: PNG @ 2× (per buona resa stampa) oppure PDF (vettoriale).
